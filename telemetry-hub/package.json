{
  "name": "bettershift-telemetry-hub",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply bettershift-telemetry --remote",
    "migrate:local": "wrangler d1 migrations apply bettershift-telemetry --local",
    "stats": "node scripts/stats.mjs",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260919.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.135.0"
  },
  "allowScripts": {
    "esbuild": true,
    "workerd": true
  }
}
